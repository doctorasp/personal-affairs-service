@model PersonalAffairs.WEB.Models.WorkerViewModel
@{
    ViewBag.Title = "AddWorker";
}

<br />
<h2 class="alert alert-info">Add Unit</h2>
@using (Html.BeginForm("AddUnit", "Unit", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
<div class="form-horizontal">
    <div class="form-group">
        <div class="col-md-2">
            <label>
                Name
            </label>
        </div>
        <div class="col-md-10">
            <input type="text" class="form-control" name="Name"/>
        </div>
    </div>
    

    <div class="form-group">
        <div class="col-md-offset-10 col-md-1">
            <input type="submit" value="Додати" class="btn btn-success" />
        </div>
    </div>
</div>
}